
#ifndef STRUCT_H
#define STRUCT_H

// datatype to return the result of a partial comparison
enum boolean{
    True,
    False,
    Undefined,
    Useless
};

struct memo{
    bool useful1;
    boolean useful2;
};
#endif
